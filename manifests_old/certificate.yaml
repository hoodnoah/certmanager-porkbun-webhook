apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: noah-hood.io
  namespace: default
spec:
  secretName: porkbun-webhook-tls
  issuerRef:
    name: porkbun-issuer
    kind: ClusterIssuer
  dnsNames:
    - noah-hood.io