apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: porkbun-solvers.acme.noah-hood.io  # Format: <plural>.<group>
spec:
  group: acme.noah-hood.io           # API group (matches your webhook's group)
  scope: Cluster                     # Typically "Namespaced" or "Cluster"
  names:
    plural: porkbun-solvers                # Plural name of the resource
    singular: porkbun-solver                # Singular name
    kind: PorkbunSolver                    # Kind (used in manifests)
    shortNames:                      # Optional short names
    - pb-solver
  versions:
  - name: v1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            type: object
            properties:
              # Define your Porkbun solver configuration here
              # Example fields (adjust based on your webhook's needs):
              apiKeySecretRef:
                type: object
                properties:
                  name:
                    type: string
                  key:
                    type: string
              secretKeySecretRef:
                type: object
                properties:
                  name:
                    type: string
                  key:
                    type: string